개발 및 테스트 환경: Ubuntu (Linux) / Windows 11

언어 및 버전: Python 3.x (Standard Library dataclasses 사용)

필수 라이브러리: numpy, matplotlib, scipy


problem1-(1)
실행 방법
problem1.py 코드의 맨 밑에 있는 main()함수에서, n의 값을 필요에 따라 수정하여 실행한다.

problem2-(2)
실행 방법
problem1.py 파일에서 함수를 불러왔으므로, 해당 코드를 먼저 실행 한 후 problem2.py를 실행한다. 이때 맨 밑의 main()함수에서, mode에 들어갈 값을 필요에 따라 수정하여 실행한다.

problem3-(1)
실행 방법
problem1.py 코드를 먼저 실행 한 후 problem3.py를 실행한다. 맨 밑의 main()함수에서, mode에 들어갈 값을 필요에 따라 수정하여 실행한다.

problem3-(4)
실행 방법
problem1.py 코드를 먼저 실행한 후 problem3.py를 실행한다. 이때 맨 밑의 main()함수에서, 작은따옴표로 주석처리된 for문에서 주석 처리를 제거하고, 그 위의 dat_output(p,mode=1)를 주석처리 한 후 실행한다.

problem4-(1)
실행 방법
problem4.py 코드의 맨 밑에 있는 main()함수에서, n의 값을 필요에 따라 수정하여 실행한다.
